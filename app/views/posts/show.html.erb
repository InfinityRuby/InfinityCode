<h1><%=@post.title%></h1>
<p><%=@post.content%></p>

<%= link_to '編輯', edit_post_path(@post) %>
<%= link_to '刪除', post_path(@post), method: :delete , data: { confirm: "Are you sure you want to do this?" } %>
<%= link_to "Home", root_path %>